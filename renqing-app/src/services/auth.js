// Auth service
export default {
  login() {},
  logout() {}
}

export const login = (credentials) => {
  // 登录逻辑
};

export const logout = () => {
  // 注销逻辑
};

export const isAuthenticated = () => {
  // 验证是否已登录
};